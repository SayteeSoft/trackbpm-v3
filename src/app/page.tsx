
export default function Home() {
    return (
        <main className="flex-1 w-full">
            {/* 
              The Header component now contains the search bar and displays the results.
              This main area is kept for future content on the homepage.
            */}
        </main>
    );
}
